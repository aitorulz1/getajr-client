import * as React from "react";

const style = {
  position: "relative",
  backgroundColor: "#000",
  textAlign: "center",
  color: "#fada02",
  padding: "10px 0px",
};

export default function NavI() {
  return (
    <div style={style}>
      Have a look, talk to them, find a partner or get a new opportunity
    </div>
  );
}
